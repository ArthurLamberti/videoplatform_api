plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'com.arthurlamberti.videoplataform.application'

dependencies {
    implementation(project(":domain"))
    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
